{
  "name": "wfs-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "lint": "echo 'No linting required for static html'"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
